{
  "_id": "{{@ system @}}",
  "oauth2": {
    "access_token": "$SECRET(oauth_access_token)",
    "client_id": "$SECRET(oauth_client_id)",
    "client_secret": "$SECRET(oauth_client_secret)",
    "refresh_token": "$SECRET(oauth_refresh_token)",
    "token_url": "{{@ token_url @}}"
  },
  "operations": {
    "alternate_delete": {
      "method": "DELETE",
      "url": "{{ properties.datatype }}/{{ entity.id }}"
    },
    "alternate_insert": {
      "method": "POST",
      "url": "{{ properties.datatype }}"
    },
    "alternate_list": {
      "method": "GET",
      "next_page_link": "{{ body.paging.next.link }}",
      "params": {
        "since": "{{ since }}"
      },
      "url": "{{ properties.datatype }}"
    },
    "alternate_lookup": {
      "method": "GET",
      "url": "{{ properties.datatype }}/{{ entity.id }}"
    },
    "alternate_update": {
      "method": "PATCH",
      "url": "{{ properties.datatype }}/{{ entity.id }}"
    },
    "alternate_update2": {
      "method": "PUT",
      "url": "{{ properties.datatype }}/{{ entity.id }}"
    },
    "associations_lookup": {
      "method": "GET",
      "url": "crm/v4/objects/{{ properties.datatype }}/{{ entity.id }}/associations/{{ properties.toObjectType }}"
    },
    "delete": {
      "method": "DELETE",
      "url": "crm/v3/objects/{{ properties.datatype }}/{{ entity.id }}"
    },
    "insert": {
      "method": "POST",
      "url": "crm/v3/objects/{{ properties.datatype }}"
    },
    "list": {
      "method": "GET",
      "next_page_link": "{{ body.paging.next.link }}",
      "params": {
        "since": "{{ since }}"
      },
      "url": "{{ properties.datatype }}?properties={{ properties.properties }}&associations={{ properties.associations }}"
    },
    "lookup": {
      "method": "GET",
      "params": {
        "associations": "{{ properties.associations }}",
        "properties": "{{ properties.properties }}"
      },
      "url": "crm/v3/objects/{{ properties.datatype }}/{{ entity.id }}"
    },
    "update": {
      "method": "PATCH",
      "url": "crm/v3/objects/{{ properties.datatype }}/{{ entity.id }}"
    }
  },
  "type": "system:rest",
  "url_pattern": "https://api.hubapi.com/%s",
  "verify_ssl": true
}