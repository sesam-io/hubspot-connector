{
  "_id": "{{@ system @}}-{{@ datatype @}}-collect",
  "namespaced_identifiers": false,
  "source": {
    "operation": "{{@ datatype @}}-list",
    "system": "{{@ system @}}",
    "type": "rest"
  },
  "transform": {
    "type": "dtl",
    "rules": {
      "default": [
        ["copy", "*", "additionalCurrencies"],
        ["comment", "IS-16923"],
        ["add", "additionalCurrencies",
          ["sorted", "_S.additionalCurrencies"]
        ]
      ]
    }
  },
  "type": "pipe"
}